dependencies {
    api project(":rollbar-web")
    //We can't update struts to version 7+ because it uses caffeine version 3+ which remove Java 8
    implementation 'org.apache.struts:struts2-core:6.8.0'
}
